version: "3"

services:
  mongo:
    image: mongo:latest
    network_mode: host
  server:
    build: server/
    command: npm start
    depends_on:
      - mongo
    network_mode: host
  client:
    build: client/
    command: npm start
    depends_on:
      - server
    network_mode: host